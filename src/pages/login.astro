---
import Layout from "../layouts/Layout.astro";
---

<Layout
	title="Acceso Clientes | MBC Predictive"
	description="Acceso exclusivo para clientes de MBC Predictive. Ingresa a tu panel de control para gestionar tus servicios de mantenimiento predictivo."
	noindex={true}
>
    <div
        class="min-h-screen flex items-center justify-center bg-[#08080A] relative overflow-hidden py-20"
    >
        <!-- Background Glow -->
        <div class="absolute inset-0 overflow-hidden pointer-events-none">
            <div
                class="absolute top-[-10%] left-[-10%] w-[40%] h-[40%] bg-[#31B0FF]/10 blur-[120px] rounded-full"
            >
            </div>
            <div
                class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-[#0099cc]/10 blur-[120px] rounded-full"
            >
            </div>
        </div>

        <div class="w-full max-w-md pt-20 relative z-10">
            <!-- Card -->
            <div
                class="bg-[#11161d] border border-white/10 rounded-2xl p-8 shadow-2xl relative group overflow-hidden"
            >
                <!-- Glow effect -->
                <div
                    class="absolute -top-20 -right-20 w-40 h-40 bg-[#31B0FF]/10 rounded-full blur-[50px] group-hover:bg-[#31B0FF]/20 transition-all duration-500 pointer-events-none"
                >
                </div>
                <!-- Header -->
                <div class="text-center mb-8">
                    <h1 class="text-3xl font-bold text-white mb-2">
                        Bienvenido
                    </h1>
                    <p class="text-gray-400 text-sm">
                        Ingresa a tu panel de control MBC Predictive.
                    </p>
                </div>

                <!-- Form -->
                <form class="space-y-6">
                    <div class="space-y-2">
                        <label
                            for="email"
                            class="text-xs font-medium text-[#31B0FF] uppercase tracking-wider"
                            >Correo Corporativo</label
                        >
                        <input
                            type="email"
                            id="email"
                            class="w-full bg-[#08080A] border border-white/10 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-[#31B0FF] transition-colors"
                            placeholder="usuario@empresa.com"
                        />
                    </div>

                    <div class="space-y-2">
                        <label
                            for="password"
                            class="text-xs font-medium text-[#31B0FF] uppercase tracking-wider"
                            >Contraseña</label
                        >
                        <input
                            type="password"
                            id="password"
                            class="w-full bg-[#08080A] border border-white/10 rounded-lg px-4 py-3 text-white focus:outline-none focus:border-[#31B0FF] transition-colors"
                            placeholder="••••••••"
                        />
                    </div>

                    <button
                        type="submit"
                        class="w-full bg-[#31B0FF] hover:bg-[#2094df] text-white font-bold py-3.5 rounded-lg transition-all duration-300 transform hover:-translate-y-0.5 shadow-lg shadow-[#31B0FF]/25"
                    >
                        Iniciar Sesión
                    </button>
                </form>

                <!-- Registration Notice -->
                <div class="mt-8 pt-6 border-t border-white/5 text-center">
                    <p class="text-gray-500 text-sm mb-2">
                        ¿No tienes una cuenta?
                    </p>
                    <div
                        class="bg-[#31B0FF]/5 border border-[#31B0FF]/20 rounded-lg p-4"
                    >
                        <p class="text-xs text-gray-300 leading-relaxed">
                            El acceso es exclusivo para clientes activos. Para
                            solicitar credenciales, contacta a soporte:
                            <a
                                href="mailto:soporte@mbcpredictive.com"
                                class="block mt-1 text-[#31B0FF] font-medium hover:underline"
                                >soporte@mbc.com</a
                            >
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</Layout>
